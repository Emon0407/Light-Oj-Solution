#include<stdio.h>
int main()
{
    int i,n,num,a,b;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        scanf("%d",&num);
        a=num/2;
        b=num-a;
        printf("%d %d\n",a,b);
    }
    return 0;
}
